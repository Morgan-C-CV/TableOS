<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude APK files from cloud backup -->
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <!-- Exclude APK files from device transfer -->
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>